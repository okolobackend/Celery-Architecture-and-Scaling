Заготовка под следующую статью.

Что нужно будет сделать по докеру.
- `celery worker -c 2` внутри контейнера(вертикальное) vs `docker-compose up --scale`(горизонтальной)).
- Показать интеграцию с Kubernetes (HPA — Horizontal Pod Autoscaler), 
который следит за длиной очереди в брокере и выполняет `kubectl scale`.
- cgroups/Kubernetes limits, чтобы изолировать веб-бекенд от селери.  
- отдельные ноды/пулы селери, если нагрузка нестабильна.  
